import styled from "styled-components";

export const AppLayoutStyles = styled.main`
  background: #fefefe;
  border-radius: 8px;
  width: 100%;
  max-width: 450px;
  box-shadow: rgba(0, 0, 0, 0.35) 0px 5px 15px;

`;
